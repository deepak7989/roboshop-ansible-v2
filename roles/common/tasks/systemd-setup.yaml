- name: copy catalogue service
  template:
    src: catalogue.service.j2
    dest: /etc/systemd/system/catalogue.service
- name: daemon reload
  systemd_service:
    name: catalogue
    daemon_reload: true